/* 
    String
    Number
    Bigint
    Boolean
    Undefined
    Null
    Symbol
    Object
*/

// Numbers:
let length = 16;
let weight = 7.5;

// Strings:
let color = "Yellow";

// Booleans
let x = true;
let y = false;

// Object:
const person = { firstName: "John", lastName: "Doe" };

// Array object:
const cars = ["Saab", "Volvo", "BMW"];

// Date object:
const date = new Date("2022-03-25");

// Symbol
let sym = Symbol("description");

// BigInt
let bigInt = BigInt(999999999999999999999999999999999);
let anotherBigInt = 98989898989898989898989898989n;

// Null
let c = null;

// undefined
let z = undefined;

// --------------- Array Methods ----------------

/* 

    Array length
    Array toString()
    Array at() - instead of arr[0] , arr.at(0)
    Array join() - Joins all array elements into a string with a specified separator.
    Array pop()
    Array push()

    Array shift() -  Removes and returns the first element of an array.
    Array unshift() - Adds one or more elements to the start of an array.
    Array delete() - Removes an element at a specific index, leaving undefined in its place.
    Array concat() - Merges two or more arrays into a new array.
    Array copyWithin() - Copies part of an array to another location within the same array.
    Array flat() - Flattens nested arrays into a single array.
    Array splice() -  Adds/removes elements at a specified index.
    Array toSpliced() - Returns a new array with elements added or removed without modifying the original array.
    Array slice() - Returns a shallow copy of a portion of an array.

*/
